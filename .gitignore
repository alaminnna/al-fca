node_modules/
.al-fca-e2ee/

# Secrets and local credentials — never commit
cookie.txt
e2ee_device.json
config.json
appstate.json
outbound_frames.log
*.log

# Mirror .npmignore — keep GitHub clean
.replit
replit.nix
generated-icon.png
li.js

# Local-only: example bots, tests — never push
bot.js
e2eebot.js
test/

# Vendored build artifacts (runtime JS stays, maps + TS sources go)
e2ee/vendor/**/*.map
e2ee/vendor/**/*.ts

# Agent / tool scratch — never commit
.omo/
.codegraph/
.kilo/
.github/
